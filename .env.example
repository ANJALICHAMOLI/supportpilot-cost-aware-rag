# Copy this file to a new file named ".env" (same folder) and fill in your real keys.
# .env is gitignored and will never be committed - .env.example IS committed so
# anyone cloning the repo knows exactly which environment variables they need,
# without ever seeing a real secret.
#
# BOTH keys below are 100% FREE to obtain - no credit card required for either.
#
# Get a free Groq API key at: https://console.groq.com/keys
